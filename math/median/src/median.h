#ifndef median_H_
#define median_H_

#include <vector>
#include <string>

using std::vector;
using std::string;

class median {
public:
    int findMedian(vector<int> nums);
    int findKthSmallest(vector<int> nums, int k);
private:

};

#endif
