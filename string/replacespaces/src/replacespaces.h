#ifndef replacespaces_H_
#define replacespaces_H_

#include <vector>
#include <string>

using std::vector;
using std::string;

class replacespaces {
public:
    void dummy();

    void ReplaceSpaces(string &str, const string replace);

private:

};

#endif
