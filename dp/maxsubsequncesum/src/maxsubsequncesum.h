#ifndef maxsubsequncesum_H_
#define maxsubsequncesum_H_

#include <vector>
#include <string>
using std::vector;
using std::string;

class maxsubsequncesum {
public:
	void dummy();
	int maxsubsequencesum(vector<int>& nums);
private:

};

#endif
