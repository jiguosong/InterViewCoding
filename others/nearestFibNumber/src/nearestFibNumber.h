#ifndef nearestFibNumber_H_
#define nearestFibNumber_H_

#include <vector>
#include <string>
using std::vector;
using std::string;

class nearestFibNumber {
public:
	void dummy();
	unsigned long long FindNearestFibNumber(unsigned int n);
private:

};

#endif
