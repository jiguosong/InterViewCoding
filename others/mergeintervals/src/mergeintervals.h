#ifndef mergeintervals_H_
#define mergeintervals_H_

#include <vector>
#include <string>

using std::vector;
using std::string;

class mergeintervals {
public:
    vector<std::pair<int, int>> MergeInterval(vector<std::pair<int, int>> &intervals);
private:

};

#endif
