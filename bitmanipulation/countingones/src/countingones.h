#ifndef countingones_H_
#define countingones_H_

#include <vector>
#include <string>

using std::vector;
using std::string;

class countingones {
public:
    int countOnes(int n);
    int countOnes2(int n);
private:

};

#endif
