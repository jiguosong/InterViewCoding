#ifndef rotateimage_H_
#define rotateimage_H_

#include <vector>
#include <string>
using std::vector;
using std::string;

class rotateimage {
public:
	void dummy();
	void RotateNNImage(vector<vector<int>> &image);
private:

};

#endif
